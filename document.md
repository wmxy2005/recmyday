# Rec My Day 产品说明

Rec My Day 用于记录每天投入的时间，并在本地 SQLite 中保存记录。

## 业务规则

- 每条记录归属到自然日期，`day_key` 格式为 `YYYY-MM-DD`。
- “一天开始时间”不改变记录归属日期，只用于计算当天分钟数。
- 当当前时间早于一天开始时间时，当天分钟数按 `0` 处理。

## 页面

- 首页：展示当天记录、最近记录，并提供记录按钮。
- 统计页：按月份展示总分钟数、日历视图和单日记录明细。
- 设置页：配置一天开始时间、记录单位、最近记录数量、分段记录模式，并提供导入导出。

## 数据

- `settings` 保存本地设置。
- `day_records` 保存记录明细。
- 导出文件包含应用标识、schema 版本、导出时间、记录数量、记录内容和 checksum。
